# Email Resolution Dump Transfer - Bead cg-13m18

## Task Completion Summary

Successfully transferred and verified the email_resolution table dump from the queue-api pod to the local filesystem on ex44.

## Transfer Details

- **Source Pod:** queue-api-c5894c469-p9rhr (ord-devimprint cluster, commitgraph namespace)
- **Source Path:** /tmp/email_resolution_dump.sql
- **Local Path:** /tmp/email_resolution_dump.sql
- **Transfer Command:**
  ```bash
  kubectl --kubeconfig=/home/coding/.kube/ord-devimprint-admin.kubeconfig cp \
    commitgraph/queue-api-c5894c469-p9rhr:/tmp/email_resolution_dump.sql \
    /tmp/email_resolution_dump.sql -c queue-api
  ```

## Verification Results

### File Size & Format
- **File Size:** 150M (150,532,787 bytes)
- **Format:** SQLite .dump format (SQL with CREATE TABLE + INSERT statements)
- **Structure:**
  - PRAGMA foreign_keys=OFF;
  - BEGIN TRANSACTION;
  - CREATE TABLE with full schema
  - INSERT statements (966,679 records)
  - COMMIT;

### Row Count
- **Total Records:** 966,679 INSERT statements
- **Verification:** exceeds 365K+ requirement ✅
- **SQLite Import Test:** Successfully imported and counted 966,679 rows ✅

### Schema Verification
All expected columns present in CREATE TABLE statement:
1. author_email (TEXT, PRIMARY KEY)
2. github_login (TEXT)
3. provider (TEXT, NOT NULL DEFAULT 'github')
4. status (TEXT, NOT NULL DEFAULT 'pending', with CHECK constraint)
5. priority (INTEGER, NOT NULL DEFAULT 0)
6. is_alias_candidate (INTEGER, NOT NULL DEFAULT 0)
7. claimed_by (TEXT)
8. claimed_at (TEXT)
9. lease_expires_at (TEXT)
10. attempted_at (TEXT)
11. created_at (TEXT, NOT NULL DEFAULT datetime('now'))
12. updated_at (TEXT, NOT NULL DEFAULT datetime('now'))

All 12 columns match the schema documentation ✅

### Format Readability
- **SQLite Import:** Successfully tested with sqlite3 .read command ✅
- **Sample Data:** First rows show valid INSERT statements with all columns populated
- **File Integrity:** Properly terminated with COMMIT; ✅

## Acceptance Criteria Status

- [x] Dump file transferred via kubectl cp from queue-api pod to local filesystem
- [x] Local file size is non-zero and appears complete (150M)
- [x] Row count verified at 365K+ rows (actual: 966,679)
- [x] All expected columns present (12 columns verified)
- [x] Format is readable (SQLite .dump imports successfully)
- [x] File path and verification results recorded in this bead's comments

## Files Generated

- `/tmp/email_resolution_dump.sql` - 150M SQLite dump file with 966,679 email resolution records

## Next Steps

The dump file is now available locally and ready for:
- Loading into PostgreSQL via the ingest path
- Analysis and transformation
- Backup and archival
