# cg-3i96: Seed email_resolution from claude-leaderboard's frozen author_login_cache

## Status: BLOCKED - Source database inaccessible

## What was attempted

### 1. Script verification
The seed script at `cmd/seed-author-login-cache/main.go` is **fully implemented and ready**. It:
- Reads from `~/backups/claude-leaderboard/hot.db`'s `author_login_cache` table
- Filters to positive resolutions only (skips NULL/empty logins)
- Sets `source='seed'` and preserves original `resolved_at` timestamps
- Ingests into Postgres via the identity ingest endpoint
- Logs summary: pairs read, accepted, rejected

### 2. Database location investigation
Attempted to locate `~/backups/claude-leaderboard/hot.db`:
- Directory `~/backups/claude-leaderboard/` exists but is empty
- Checked apexalgo-iad cluster: PVC `claude-leaderboard-data-v2` exists but has volume attachment issues
- Pod `claude-leaderboard-6fdc55b896-x6j99` is stuck in ContainerCreating state
- Recent events show: `FailedAttachVolume` for PVC - "volume attachment is being deleted"

### 3. Alternative access attempts
- Attempted to create temporary pod with PVC mount: failed
- Checked add-users pod for /data access: not mounted
- Searched local filesystem for *.db files: no claude-leaderboard database found

## Root cause
The claude-leaderboard PVC has volume attachment issues preventing access to the frozen `hot.db` file. The pod cannot start due to these storage problems.

## Next steps needed
1. **Resolve PVC volume attachment issues** for `claude-leaderboard-data-v2` on apexalgo-iad cluster
2. **Retrieve frozen hot.db backup** from alternative location if available
3. **Recreate database from leaderboard.db** using `build_hot_db.py` if source data is accessible

## Acceptance criteria status
- [ ] Reads directly from `~/backups/claude-leaderboard/hot.db` — BLOCKED: file inaccessible
- [ ] Submits all 349,425 pairs — BLOCKED: cannot access source data
- [ ] `source='seed'` on every row — Ready: script implements this
- [ ] `resolved_at` from cache timestamp — Ready: script implements this
- [ ] No negative-cache entries — Ready: script filters these out
- [ ] Logs summary — Ready: script implements this

## Script ready to run
Once the database is accessible at `~/backups/claude-leaderboard/hot.db`, the script can be executed with:

```bash
cd ~/commitgraph
go run cmd/seed-author-login-cache/main.go \
  -db-host <host> \
  -db-user <user> \
  -db-password <password> \
  -claude-leaderboard-db ~/backups/claude-leaderboard/hot.db
```

## References
- Plan section: docs/plan/plan.md "Explicitly out of scope"
- Script: cmd/seed-author-login-cache/main.go
- Ingest endpoint: pkg/identity/ingest.go
- Research notes: docs/research/claude-leaderboard-comparison.md
