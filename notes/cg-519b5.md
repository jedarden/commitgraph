# Bead cg-519b5: Email Resolution Table Extraction

## Status: BLOCKED - Expired Kubeconfig

## Findings

### Queue-API Pod Location
- **Cluster:** ord-devimprint
- **Namespace:** commitgraph
- **Pod:** queue-api-c5894c469-p9rhr
- **Status:** Running (2/2 containers), age 5d11h

### Blocker
The admin kubeconfig for ord-devimprint (`/home/coding/.kube/ord-devimprint-admin.kubeconfig`) does not exist on this machine. This kubeconfig uses an OIDC token from the Rackspace Spot UI that expires every ~3 days.

### Available Access
- **Read-only proxy:** `kubectl --server=http://kubectl-proxy-ord-devimprint:8001` - Works for listing/getting resources
- **Admin kubeconfig:** Missing - Required for `kubectl exec` operations

## Required Action

To proceed with the database extraction, the admin kubeconfig must be regenerated:

1. Log in to the Rackspace Spot UI (ord-devimprint cloudspace)
2. Navigate to the OIDC token generation for cloudspace-admin
3. Download the new kubeconfig to `/home/coding/.kube/ord-devimprint-admin.kubeconfig`

## Planned Extraction Commands (Once Kubeconfig Available)

```bash
# 1. Verify pod access
kubectl --kubeconfig=/home/coding/.kube/ord-devimprint-admin.kubeconfig \
  get pods -n commitgraph -l app=queue-api

# 2. Dump the email_resolution table to SQLite dump format
kubectl --kubeconfig=/home/coding/.kube/ord-devimprint-admin.kubeconfig \
  exec -n commitgraph queue-api-c5894c469-p9rhr -c queue-api -- \
  sqlite3 /data/queue-api.db ".dump email_resolution" > email_resolution_dump.sql

# OR: Export to CSV (alternative format)
kubectl --kubeconfig=/home/coding/.kube/ord-devimprint-admin.kubeconfig \
  exec -n commitgraph queue-api-c5894c469-p9rhr -c queue-api -- \
  sqlite3 /data/queue-api.db \
  "SELECT * FROM email_resolution" > email_resolution.csv

# 3. Verify row count matches expected 365K+ rows
wc -l email_resolution_dump.sql  # or email_resolution.csv
```

## Database Location Assumptions
Based on typical queue-api patterns:
- Database file: `/data/queue-api.db` (may vary - need to verify)
- Container: `queue-api` (one of the 2 containers in the pod)

## Verification Needed
Once kubeconfig is available, confirm:
1. Exact database path in the container
2. Which container holds the database (may be sidecar vs main)
3. SQLite3 availability in the container (may need to use sqlite3 binary)

## Timestamp
- Discovery attempt: 2026-08-06
